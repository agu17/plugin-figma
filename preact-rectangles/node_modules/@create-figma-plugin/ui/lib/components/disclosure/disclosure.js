import { h } from 'preact';
import { useCallback } from 'preact/hooks';
import { IconCaretRight16 } from '../../icons/icon-16/icon-caret-right-16';
import styles from './disclosure.css';
export function Disclosure({ children, onClick, open, propagateEscapeKeyDown = true, title, ...rest }) {
    const handleKeyDown = useCallback(function (event) {
        if (event.key !== "Escape") {
            return;
        }
        if (propagateEscapeKeyDown === false) {
            event.stopPropagation();
        }
        event.currentTarget.blur();
    }, [propagateEscapeKeyDown]);
    return (h("label", { class: styles.disclosure }, h("input", { ...rest, checked: open === true, class: styles.input, onClick: onClick, onKeyDown: handleKeyDown, tabIndex: 0, type: "checkbox" }), h("div", { class: styles.title }, h("div", { class: styles.icon }, h(IconCaretRight16, null)), title), h("div", { class: styles.children }, children)));
}
//# sourceMappingURL=disclosure.js.map